[package]
publish = false
name = "berserker-fire-web-demo"
version = "0.2.15"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
cvkg-core.workspace = true
cvkg-render-gpu.workspace = true
glam = "0.29"

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
