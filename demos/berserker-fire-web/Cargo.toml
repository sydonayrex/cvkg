[package]
publish = false
name = "berserker-fire-web-demo"
version = "0.2.9"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
cvkg-core = { path = "../../cvkg-core" }
cvkg-render-gpu = { path = "../../cvkg-render-gpu" }
glam = "0.29"

[package.metadata.wasm-pack.profile.release]
wasm-opt = false
