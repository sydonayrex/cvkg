name: CVKG CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  # Version 4 feature: Reusable workflows with matrix including macOS, Linux, Windows
  build:
    name: Build Project
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    outputs:
      build-success:
        description: 'Whether the build succeeded'
        value: ${{ jobs.build.outputs.success }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Call reusable build workflow
      uses: ./.github/workflows/reusable/build.yml
      with:
        os: ${{ matrix.os }}
        rust-toolchain: stable
        build-args: --workspace

  test:
    name: Test Suite
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Call reusable test workflow
      uses: ./.github/workflows/reusable/test.yml
      with:
        os: ${{ matrix.os }}
        rust-toolchain: stable
        test-args: --workspace

  # Additional jobs for comprehensive CI (run on ubuntu-latest for consistency)
  clippy:
    name: Clippy Lint
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: clippy
    
    - uses: actions/cache@v4
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-registry-
    
    - uses: actions/cache@v4
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-index-
    
    - name: Run Clippy
      run: cargo clippy --workspace -- -D warnings

  fmt:
    name: Format Check
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt
    
    - name: Check formatting
      run: cargo fmt --workspace -- --check

  # Release job: Triggered on tags (e.g., v0.1.16)
  release:
    name: Build & Upload Release Assets
    needs: [build, test, clippy, fmt]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    permissions:
      contents: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
    
    - name: Install Linux Dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y libx11-dev libxcursor-dev libxrandr-dev libxi-dev libasound2-dev libfontconfig1-dev
    
    - name: Build release binaries
      run: |
        cargo build --release -p cvkg-cli
        cargo build --release -p berserker
        cargo build --release -p cvkg-webkit-server
    
    - name: Package Assets (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        mkdir -p assets
        # Use common names with OS suffix
        cp target/release/cvkg assets/cvkg-${{ runner.os }}
        cp target/release/berserker assets/berserker-${{ runner.os }}
        cp target/release/cvkg-webkit-server assets/cvkg-webkit-server-${{ runner.os }}
        
        cd assets && tar -czf ../cvkg-${{ runner.os }}.tar.gz *
    
    - name: Package Assets (Windows)
      if: runner.os == 'Windows'
      run: |
        mkdir assets
        copy target\release\cvkg.exe assets\cvkg-Windows.exe
        copy target\release\berserker.exe assets\berserker-Windows.exe
        copy target\release\cvkg-webkit-server.exe assets\cvkg-webkit-server-Windows.exe
        Compress-Archive -Path assets\* -DestinationPath cvkg-Windows.zip
    
    - name: Upload Release Assets
      uses: softprops/action-gh-release@v2
      with:
        files: |
          cvkg-*.tar.gz
          cvkg-*.zip
        tag_name: ${{ github.ref_name }}
        name: Release ${{ github.ref_name }}
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Version 4 feature: Security scanning
  security:
    name: Security Scan
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install cargo-audit
      run: cargo install cargo-audit
    
    - name: Run security audit
      run: cargo audit
    
    - name: Run cargo-deny
      uses: EmbarkStudios/cargo-deny-action@v1
