# cvkg-vdom

**cvkg-vdom** provides the virtual DOM bridge for CVKG's web and remote backends.

## Features
- **Lightweight Nodes**: Minimal representation of the view tree for serialization.
- **VDom Diffing**: Efficient patch generation for DOM updates.
- **Remote Rendering**: Support for the Inspector WebSocket protocol.
